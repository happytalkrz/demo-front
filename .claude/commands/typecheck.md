TypeScript 타입 체크를 실행하고 에러가 있으면 수정합니다.

## 작업
1. `npx tsc --noEmit` 실행
2. 에러가 있으면 분석 후 수정
3. 수정 후 다시 `npx tsc --noEmit`으로 확인
4. 모든 에러가 해결될 때까지 반복

## 규칙
- `any` 타입 사용 최소화
- 타입 정의는 `src/types/` 디렉토리에 배치
- 기존 코드 스타일과 일관성 유지
