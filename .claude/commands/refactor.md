지정된 파일이나 컴포넌트를 리팩토링합니다.

## 입력
$ARGUMENTS — 대상 파일 경로 또는 설명 (예: `layouts/MainLayout`, `사이드바 메뉴 데이터 분리`)

## 규칙
- 기능 변경 없이 구조만 개선
- 컴포넌트 분리 시 같은 디렉토리 또는 적절한 하위 디렉토리에 배치
- 반복 코드 → 공용 컴포넌트 또는 커스텀 훅으로 추출
- import 경로 정리
- 리팩토링 후 `npx tsc --noEmit`으로 타입 체크 확인
- 변경 사항 요약을 마지막에 출력
